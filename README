커스텀 어노테이션 추가하는 방법:

1. custom-annotations-api 모듈에 사용할 어노테이션 추가.
2. custom-annotations 모듈에 Handler 추가 후, CustomAnnotationsPlugin 에 등록.
3. 위의 두 모듈을 jar로 빌드. 그래들 빌드가 완료되면 build/libs에 생성됨.
4. 두 jar 파일을 복사 후, 프로젝트에서 사용!

추가된 커스텀 어노테이션:

@IntervalClick -> 중복 클릭 막기 위해 사용. delay 값으로 인터벌 시간을 지정 할 수 있음.